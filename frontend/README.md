# Frontend Workspace

The main project documentation lives in the repository root:

- [../README.md](../README.md)

This `frontend/` workspace contains the Next.js app, API routes, Prisma schema, and the indexer process that syncs on-chain events into PostgreSQL.
